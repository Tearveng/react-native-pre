import React from 'react';
import UserRegister from '../user/userRegister';
import {Box} from 'native-base';

const Home = () => {
  return (
    <Box m={10}>
      <UserRegister />
    </Box>
  );
};

export default Home;
